const httpStatus = require('http-status');

const loginWithGoogle = async (email, password) => {
    console.log("PRint auth userServices code",email, password)
  return user;
};


module.exports = {
  loginWithGoogle,
};
