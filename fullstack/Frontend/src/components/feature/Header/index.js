import React from "react";
import { Box, Typography } from "@mui/material";

export const HeaderComponent = () => {
  return (
    <Box>
      <Typography> HeaderComponent</Typography>
    </Box>
  );
};
